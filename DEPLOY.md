# 바로 실행하기

Firebase 연결값은 이미 입력되어 있습니다.

## 먼저 확인할 것

Firebase Console에서 아래 두 가지가 켜져 있어야 합니다.

1. Authentication > 로그인 방법 > 익명 = 사용 설정
2. Realtime Database > 규칙 = 아래 내용으로 게시

```json
{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
```

## 실행 방법

이 프로젝트는 브라우저용 ES Module을 사용하므로
`index.html` 파일을 더블클릭해서 `file://`로 직접 여는 것보다
정적 호스팅에 올려 실행하는 것이 좋습니다.

추천:
- GitHub Pages
- Netlify
- Firebase Hosting

## 정상 작동 테스트

1. 사이트 접속
2. 플레이어 이름 입력
3. 새 방 만들기
4. 6자리 방 코드 확인
5. 다른 브라우저나 시크릿 창에서 같은 사이트 접속
6. 다른 이름 입력
7. 방 코드 입력 후 참가
8. 서로 다른 국가 선택
9. 방장이 게임 시작
10. 공개 성명 / 비밀 외교 / 군사 행동이 양쪽 화면에 반영되는지 확인

## 현재 버전
- 4개 국가
- 1개 기본 시나리오
- 방 생성/참가
- 국가 선택
- 공개 성명
- 협정 제안
- 원조
- 제재
- 비밀 외교
- 병력 증강
- 최후통첩
- 전쟁 선포
- 세계 긴장도
- 국제 뉴스 피드
- 라운드 진행
- 세계 사건 발생
